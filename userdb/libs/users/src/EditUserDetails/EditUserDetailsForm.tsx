import { User } from "@react-workshop/userdb-api-client"

export interface EditUserDetailsFormProps {
	user: User
}

export const EditUserDetailsForm = (props: EditUserDetailsFormProps) => {
	return <div>Test</div>
}
